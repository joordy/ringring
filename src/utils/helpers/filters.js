export const filterData = (rawData) => {
  return rawData
  // return rawData.map((item) => {
  //   return {
  //     distance: item.properties.distance,
  //     // date: item.properties.distance,
  //   }
  // })
  // // console.log(rawData)
  // return rawData
  // // const abcd = () => {
  // //   rawData.forEach((elem) => console.log(typeof elem.properties.distance))
  // // }
  // // abcd()
}
